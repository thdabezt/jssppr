# Required for BACKEND = "sat"
python-sat>=1.8.dev13
pypblib>=0.0.4

# Required for BACKEND = "cplex_cp1", "cplex_cp2", or "cplex_mip".
# docplex is on PyPI; the cplex package and the cpoptimizer executable come from
# IBM CPLEX Optimization Studio and need a licence that fits the model size.
# docplex>=2.25
# cplex>=22.1

# Required for BACKEND = "gurobi".
# The licence bundled with the wheel is limited to 2000 variables and 2000
# constraints, which every instance in dataset/ exceeds, so a full Gurobi
# licence must be installed as well.
# gurobipy>=11.0
